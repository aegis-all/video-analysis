# 動画レビュー・フィードバック Web アプリの依存ライブラリ
#
#   python -m pip install -r requirements.txt
#
# DB は Python 標準の sqlite3 を使うためインストール不要。

Flask>=3.0
pykakasi>=2.2

# ログイン（ID + パスワード + Google Authenticator）
pyotp>=2.9
qrcode>=7.4
pillow>=10.0
cryptography>=42.0

# この PC で https を使うためのサーバー。
# Flask 付属の開発サーバーは TLS の握手を受付ループの中で行うため、
# ブラウザが先回りして張る接続だけで全体が止まってしまう。
cheroot>=10.0

# 本番用の WSGI サーバー（プロキシの後ろに置く場合に使う）
waitress>=3.0

# スクリーンショット生成（auto_screenshot.py）
# サーバーには画面が無いので headless 版を使う。
# 通常版は libGL などを別途入れないと import できない。
opencv-python-headless>=4.9
numpy>=1.26
